import string from './models/Search';
// import { add as a, multiply as m, ID } from './views/searchView';
import * as searchView from './views/searchView';

console.log(string);
console.log(searchView.add(1,2), searchView.multiply(2, 2), searchView.ID + 2);